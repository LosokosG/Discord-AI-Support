---
// In MVP, we're redirecting the main page to the dashboard directly
// In the future, this could be a landing page or a login page

// Redirect to dashboard
return Astro.redirect("/dashboard");
---
